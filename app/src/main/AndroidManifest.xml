<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ikrima.practice.dicoding.githubuserappdicoding">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_github_user_white"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_github_user_white_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitHubUserAppDicoding"
        tools:targetApi="31">
        <activity
            android:name=".ui.contents.githubusers.features.detailuser.favoriteuser.activities.FavoriteActivity"
            android:exported="false" />
        <activity
            android:name=".ui.contents.githubusers.features.detailuser.activities.DetailGithubUserActivity"
            android:exported="false" />
        <activity
            android:name=".ui.contents.githubusers.activities.GitHubUsersActivity"
            android:exported="false" />
        <activity
            android:name=".ui.splashscreen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>